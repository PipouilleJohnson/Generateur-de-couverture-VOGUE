let circle;

function setup() {
    // Configuration initiale
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    // Dessin et mise à jour de l'animation
    noFill();
    stroke(255,255,255);
    ellipse(500, 500, windowWidth/2, windowHeight/2);
  }